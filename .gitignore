# Build artifacts
/target/
*.class
*.jar
*.war
*.iml
.settings/
.classpath
.project

# IDE Files
.idea/
.vscode/

# Node
node_modules/
dist/
*.local

# Native C++ - trailing-slash-only pattern (no other slash), so it matches
# a "build" directory at any depth, not just native/build/ at the repo root.
build/

# Local file uploads (created by application-local profile)
uploads/

# C++ Crypto key: 
/infra/secrets/*
